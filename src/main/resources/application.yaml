spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nurse_db
    username: root
    password: Oluwadarasimi4u
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
      hibernate.format_sql: true
    hibernate:
      ddl-auto: create
      show-sql: true

